import React from "react";
import { createContext } from "react"; // hookul dat(useContext care ne trebuie ulterior) ne permite primim o valoare

export const Greeting = createContext(); //initializam valoarea